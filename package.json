{
  "name": "kmi_counter",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "dead-server --host=localhost --port=4817",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "saulute",
  "license": "ISC",
  "devDependencies": {
    "dead-server": "^1.0.9"
  }
}
